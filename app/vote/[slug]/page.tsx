import { notFound } from 'next/navigation'; import { PublicHeader } from '@/components/public-shell'; import { VoteCheckout } from '@/components/vote-checkout'; import { getLiveEvent,getPublicContestants } from '@/lib/platform-store';
export const dynamic='force-dynamic';
export default async function Vote({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const contestants=await getPublicContestants();const event=await getLiveEvent();const item=contestants.find(c=>c.slug===slug);if(!item)notFound();return <main className="site-shell"><PublicHeader/><section className="checkout"><div><p className="eyebrow">SELECT YOUR SUPPORT</p><h1>Vote for<br/><em>{item.name}.</em></h1><p>Choose your quantity and preferred provider. Every verified payment creates one auditable ledger entry and updates the public ranking.</p></div><VoteCheckout item={item} event={event}/></section></main>}
