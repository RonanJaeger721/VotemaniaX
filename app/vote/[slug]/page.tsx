import Link from 'next/link'; import { notFound } from 'next/navigation'; import { LockKeyhole } from 'lucide-react'; import { contestants,currentEvent } from '@/lib/platform-data'; import { PublicHeader } from '@/components/public-shell';
export default async function Vote({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const item=contestants.find(c=>c.slug===slug);if(!item)notFound();return <main className="site-shell"><PublicHeader/><section className="checkout"><div><p className="eyebrow">SELECT YOUR SUPPORT</p><h1>Vote for<br/><em>{item.name}.</em></h1><p>Votes are only added after the payment provider confirms success.</p></div><form className="checkout-card"><span className="checkout-avatar">{item.name.charAt(0)}</span><div><small>YOUR CONTESTANT</small><h2>{item.name}</h2><p>{currentEvent.name}</p></div><fieldset><legend>Number of votes</legend>{[1,5,10,20].map(q=><label key={q}><input type="radio" name="quantity" value={q} defaultChecked={q===1}/><span>{q}</span></label>)}</fieldset><div className="checkout-total"><span>From</span><strong>${currentEvent.price.toFixed(2)}</strong></div><button type="button" disabled><LockKeyhole size={16}/> Payment setup required</button><p className="secure-note">Ecocash and InnBucks can be connected when the new merchant credentials are supplied.</p><Link href={`/contestants/${item.slug}`}>Return to profile</Link></form></section></main>}
