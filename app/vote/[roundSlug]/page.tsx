import Link from 'next/link';import {notFound} from 'next/navigation';import {ArrowUpRight} from 'lucide-react';import {PublicFooter,PublicHeader} from '@/components/public-shell';import {RoundCountdown} from '@/components/round-countdown';import {RoundShare} from '@/components/round-share';import {getCurrentRound,getRoundBySlug,getRoundContestants,isRoundOpen} from '@/lib/platform-store';
export const dynamic='force-dynamic';export default async function RoundPage({params}:{params:Promise<{roundSlug:string}>}){const {roundSlug}=await params;const round=await getRoundBySlug(roundSlug);if(!round)notFound();const people=await getRoundContestants(round.id);const open=isRoundOpen(round);const current=await getCurrentRound();return <main className="site-shell"><PublicHeader/><section className="round-hero"><div><p className="live-pill"><i/> WEEK {String(round.weekNumber).padStart(2,'0')} · {open?'LIVE':'CLOSED'}</p><h1>{round.name}</h1><p>Seven days. One leaderboard. Back the performer you believe in.</p><RoundShare title={`Vote in ${round.name}`} endAt={round.endAt.toISOString()}/></div><RoundCountdown startAt={round.startAt.toISOString()} endAt={round.endAt.toISOString()}/></section><section className="round-roster"><header><div><small>{open?'VOTING NOW':'FINAL STANDINGS'}</small><h2>{people.length} contestants</h2></div><Link href={`/leaderboard/${round.slug}`}>Full leaderboard <ArrowUpRight/></Link></header>{people.map((p,i)=><article key={p.id}><span>{String(i+1).padStart(2,'0')}</span><div><b>{p.name}</b><small>{p.category}</small></div><strong>{p.votes} votes</strong>{open?<Link className="vote-now" href={`/vote/${round.slug}/${p.slug}`}>Vote</Link>:null}</article>)}</section>{!open&&<section className="closed-actions"><h2>This voting round has ended.</h2><Link className="primary-action" href={`/results/${round.slug}`}>View final results</Link>{current?.status==='live'&&current.id!==round.id?<Link href={`/vote/${current.slug}`}>Current voting week →</Link>:null}</section>}<PublicFooter/></main>}
