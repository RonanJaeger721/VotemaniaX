import { PublicFooter, PublicHeader } from '@/components/public-shell'; import { ContestantCard } from '@/components/contestant-card'; import { contestants } from '@/lib/platform-data';
export default function Contestants(){return <main className="site-shell"><PublicHeader/><section className="page-intro"><p className="eyebrow">DISCOVER THE LINE-UP</p><h1>Meet the<br/><em>contenders.</em></h1><p>Talent is everywhere. Momentum is earned one verified vote at a time.</p></section><section className="contestant-grid">{contestants.map((item,index)=><ContestantCard item={item} index={index} key={item.slug}/>)}</section><PublicFooter/></main>}
