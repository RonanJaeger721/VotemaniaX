import { PublicFooter, PublicHeader } from '@/components/public-shell'; import { ContestantCard } from '@/components/contestant-card'; import { getPublicContestants } from '@/lib/platform-store';
export const dynamic='force-dynamic'; export default async function Contestants(){const contestants=await getPublicContestants();return <main className="site-shell"><PublicHeader/><section className="page-intro"><p className="eyebrow">DISCOVER THE LINE-UP</p><h1>Meet the<br/><em>contenders.</em></h1><p>All active performers from the live event. Momentum is earned one verified vote at a time.</p><form className="directory-tools"><input name="search" placeholder="Search contestants" aria-label="Search contestants"/><select name="event" aria-label="Select event"><option>Current live event</option></select></form></section><section className="contestant-grid">{contestants.map((item,index)=><ContestantCard item={item} index={index} key={item.slug}/>)}</section><PublicFooter/></main>}
