import Link from 'next/link'; import { ArrowUpRight, Radio } from 'lucide-react'; import { currentEvent, contestants } from '@/lib/platform-data'; import { PublicFooter, PublicHeader } from '@/components/public-shell';
export default function Events(){return <main className="site-shell"><PublicHeader/><section className="page-intro"><p className="eyebrow">NOW HAPPENING</p><h1>Follow the<br/><em>competition.</em></h1></section><section className="event-feature"><div className="event-art"><span>V</span><div className="stage-orbit orbit-one"/></div><div className="event-copy"><p className="live-pill"><Radio size={13}/> Voting live</p><h2>{currentEvent.name}</h2><p>{currentEvent.description}</p><dl><div><dt>Contestants</dt><dd>{contestants.length}</dd></div><div><dt>Vote price</dt><dd>${currentEvent.price.toFixed(2)}</dd></div><div><dt>Closing</dt><dd>30 Sep</dd></div></dl><Link className="primary-action" href={`/events/${currentEvent.slug}`}>Enter event <ArrowUpRight size={18}/></Link></div></section><PublicFooter/></main>}
