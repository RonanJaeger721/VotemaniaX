import Link from 'next/link'; import { notFound } from 'next/navigation'; import { ArrowUpRight,Clock,Radio } from 'lucide-react'; import { ContestantCard } from '@/components/contestant-card'; import { PublicFooter,PublicHeader } from '@/components/public-shell'; import { ShareButton } from '@/components/share-button'; import { ensureSourceSnapshot,getPublicContestants } from '@/lib/platform-store'; import { getDb } from '@/db'; import { events } from '@/db/schema'; import { eq } from 'drizzle-orm';
export const dynamic='force-dynamic'; export default async function Event({params}:{params:Promise<{slug:string}>}){const {slug}=await params;await ensureSourceSnapshot();const event=(await getDb().select().from(events).where(eq(events.slug,slug)).limit(1))[0];if(!event)notFound();const contestants=await getPublicContestants();return <main className="site-shell"><PublicHeader/><section className="event-detail"><p className="live-pill"><Radio size={13}/> {event.status}</p><h1>{event.name}</h1><p>{event.description}</p><div className="event-meta"><span><Clock size={16}/> {date(event.startAt)} — {date(event.endAt)}</span><span>{event.currency} {event.votePrice.toFixed(2)} per vote</span></div><div className="hero-actions">{event.status==='live'&&<Link className="primary-action" href="/contestants">Back your favourite <ArrowUpRight size={18}/></Link>}<ShareButton title={event.name}/></div></section><section className="event-module-head"><p className="eyebrow">COMPETING NOW</p><h2>{contestants.length} contestants</h2><Link href="/leaderboard">View full leaderboard</Link></section><section className="contestant-grid compact">{contestants.map((item,index)=><ContestantCard item={item} index={index} key={item.slug}/>)}</section><section className="event-rules"><p className="eyebrow">EVENT INFORMATION</p><h2>Vote with confidence.</h2><p>Vote totals include only verified transactions. Pending, failed, expired and cancelled payments never change the ranking. The final leaderboard remains available after voting closes.</p></section><PublicFooter/></main>} function date(value:Date|null){return value?new Intl.DateTimeFormat('en-GB',{dateStyle:'long'}).format(value):'TBA'}
