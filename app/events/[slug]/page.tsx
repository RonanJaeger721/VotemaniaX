import Link from 'next/link'; import { ArrowUpRight, Clock, Radio } from 'lucide-react'; import { contestants,currentEvent } from '@/lib/platform-data'; import { ContestantCard } from '@/components/contestant-card'; import { PublicFooter, PublicHeader } from '@/components/public-shell';
export default function Event(){return <main className="site-shell"><PublicHeader/><section className="event-detail"><p className="live-pill"><Radio size={13}/> Voting live</p><h1>{currentEvent.name}</h1><p>{currentEvent.description}</p><div className="event-meta"><span><Clock size={16}/> Closes 30 September 2026</span><span>${currentEvent.price.toFixed(2)} per vote</span></div><Link className="primary-action" href="/contestants">Back your favourite <ArrowUpRight size={18}/></Link></section><section className="contestant-grid compact">{contestants.slice(0,6).map((item,index)=><ContestantCard item={item} index={index} key={item.slug}/>)}</section><PublicFooter/></main>}
