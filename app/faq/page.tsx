import { PublicFooter,PublicHeader } from '@/components/public-shell';
const faqs=[['When does my vote count?','Only after the payment provider returns a verified success. Pending, failed, cancelled and expired payments do not change the leaderboard.'],['Can I vote more than once?','Yes. Choose the quantity you want during checkout, subject to the active event settings.'],['Where can I see results?','Open the live leaderboard. Closed events remain available as results once published by the organiser.']];
export default function FAQ(){return <main className="site-shell"><PublicHeader/><section className="page-intro"><p className="eyebrow">VOTING, EXPLAINED</p><h1>Good questions.<br/><em>Clear answers.</em></h1></section><section className="faq-list">{faqs.map((f,i)=><details key={f[0]} open={i===0}><summary><span>0{i+1}</span>{f[0]}<b>+</b></summary><p>{f[1]}</p></details>)}</section><PublicFooter/></main>}
