import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import { PublicFooter, PublicHeader } from '@/components/public-shell'; import { contestants, currentEvent, totalVotes } from '@/lib/platform-data';
export default function Leaderboard(){const sorted=[...contestants].sort((a,b)=>b.votes-a.votes);return <main className="site-shell"><PublicHeader/><section className="board-head"><div><p className="live-pill">● Live leaderboard</p><h1>The crowd<br/><em>has spoken.</em></h1></div><div><span>{currentEvent.name}</span><strong>{totalVotes}</strong><small>verified votes</small></div></section><section className="podium">{sorted.slice(0,3).map((c,i)=><Link href={`/contestants/${c.slug}`} className={`podium-card place-${i+1}`} key={c.slug}><span>0{i+1}</span><b>{c.name.charAt(0)}</b><h2>{c.name}</h2><p>{c.votes} votes</p></Link>)}</section><section className="full-board">{sorted.map((c,i)=><Link href={`/contestants/${c.slug}`} className="board-row" key={c.slug}><strong>{String(i+1).padStart(2,'0')}</strong><span className="avatar">{c.name.charAt(0)}</span><div><b>{c.name}</b><small>{c.category}</small></div><span>{c.votes} votes</span><ArrowUpRight size={18}/></Link>)}</section><PublicFooter/></main>}
