import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react';
export function ContestantCard({item,index}:{item:{slug:string;name:string;category:string;votes:number},index:number}){return <article className={`contestant-card card-${index%3}`}><div className="contestant-visual"><span>{item.name.charAt(0)}</span><b>#{String(index+1).padStart(2,'0')}</b></div><div className="contestant-copy"><small>{item.category}</small><h2>{item.name}</h2><div><span>{item.votes} verified votes</span><Link href={`/contestants/${item.slug}`}>View profile <ArrowUpRight size={15}/></Link><Link className="mini-vote" href={`/vote/${item.slug}`}>Vote</Link></div></div></article>}
