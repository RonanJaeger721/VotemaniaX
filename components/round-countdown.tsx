'use client';
import {useEffect,useState} from 'react';
export function RoundCountdown({startAt,endAt}:{startAt:string;endAt:string}){const [now,setNow]=useState(0);useEffect(()=>{setNow(Date.now());const id=setInterval(()=>setNow(Date.now()),1000);return()=>clearInterval(id)},[]);if(!now)return <div className="countdown-skeleton" aria-label="Loading voting countdown"/>;const start=new Date(startAt).getTime(),end=new Date(endAt).getTime(),left=Math.max(0,end-now),open=now>=start&&now<end;const days=Math.floor(left/86400000),hours=Math.floor(left/3600000)%24,minutes=Math.floor(left/60000)%60,seconds=Math.floor(left/1000)%60;const elapsed=Math.min(100,Math.max(0,(now-start)/(end-start)*100));if(!open)return <div className="round-closed">VOTING CLOSED</div>;return <div className="countdown-wrap"><div className="countdown" aria-label={`${days} days ${hours} hours ${minutes} minutes ${seconds} seconds remaining`}>{[[days,'DAYS'],[hours,'HRS'],[minutes,'MIN'],[seconds,'SEC']].map(([value,label])=><span key={label}><b>{String(value).padStart(2,'0')}</b><small>{label}</small></span>)}</div><div className="week-progress"><i style={{width:`${elapsed}%`}}/><span>Day {Math.min(7,Math.floor((now-start)/86400000)+1)} of 7</span></div></div>}
